"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(globalThis["webpackChunkshopify_theme_starter"] = globalThis["webpackChunkshopify_theme_starter"] || []).push([["test"],{

/***/ "./src/styles/components/confetti.scss":
/*!*********************************************!*\
  !*** ./src/styles/components/confetti.scss ***!
  \*********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n// extracted by mini-css-extract-plugin\n\n\n//# sourceURL=webpack://shopify-theme-starter/./src/styles/components/confetti.scss?");

/***/ }),

/***/ "./src/js/modules/test.js":
/*!********************************!*\
  !*** ./src/js/modules/test.js ***!
  \********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var Styles_components_confetti_scss__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! Styles/components/confetti.scss */ \"./src/styles/components/confetti.scss\");\n\r\n\r\nclass ConfettiAnimation extends HTMLElement {\r\n  constructor(){\r\n    super();\r\n\r\n    this.settings = {\r\n      confettiCount: 20,\r\n      sequinCount: 10,\r\n      gravityConfetti: 0.3,\r\n      gravitySequins: 0.55,\r\n      dragConfetti: 0.075,\r\n      dragSequins: 0.02,\r\n      terminalVelocity: 3,\r\n      disabled: false,\r\n      colors: {\r\n        front: '#7b5cff', back: '#6245e0',\r\n        front: '#b3c7ff', back: '#8fa5e5',\r\n        front: '#5c86ff', back: '#345dd1'\r\n      }\r\n    };\r\n    this.elements = {\r\n      button: document.getElementById('button'),\r\n      canvas: document.getElementById('canvas'),\r\n      ctx: canvas.getContext('2d')\r\n    };\r\n\r\n    this.variables = {\r\n      confetti: [],\r\n      sequins: [],\r\n      cx: this.elements.ctx.canvas.width / 2,\r\n      cy: this.elements.ctx.canvas.height / 2,\r\n      randomRange: (min, max) => Math.random() * (max - min) + min\r\n    }\r\n\r\n    canvas.width = window.innerWidth;\r\n    canvas.height = window.innerHeight;\r\n\r\n    this.initBurst;\r\n    this.render;\r\n    this.clickButton;\r\n    this.resize;\r\n  };\r\n  \r\n  initConfettoVelocity = (xRange, yRange) => {\r\n    const x = this.variables.randomRange(xRange[0], xRange[1])\r\n    const range = yRange[1] - yRange[0] + 1\r\n    let y = yRange[1] - Math.abs(this.variables.randomRange(0, range) + this.variables.randomRange(0, range) - range)\r\n    if (y >= yRange[1] - 1) {\r\n      // Occasional confetto goes higher than the max\r\n      y += (Math.random() < .25) ? this.variables.randomRange(1, 3) : 0\r\n    }\r\n    return {x: x, y: -y}\r\n  };\r\n\r\n  Confetto = () => {\r\n    this.randomModifier = this.variables.randomRange(0, 99)\r\n    this.color = this.settings.colors[Math.floor(this.variables.randomRange(0, this.settings.colors.length))]\r\n    this.dimensions = {\r\n      x: this.variables.randomRange(5, 9),\r\n      y: this.variables.randomRange(8, 15),\r\n    }\r\n    this.position = {\r\n      x: this.variables.randomRange(canvas.width/2 - button.offsetWidth/4, canvas.width/2 + button.offsetWidth/4),\r\n      y: this.variables.randomRange(canvas.height/2 + button.offsetHeight/2 + 8, canvas.height/2 + (1.5 * button.offsetHeight) - 8),\r\n    }\r\n    this.rotation = this.variables.randomRange(0, 2 * Math.PI)\r\n    this.scale = {\r\n      x: 1,\r\n      y: 1,\r\n    }\r\n    this.velocity = this.initConfettoVelocity([-9, 9], [6, 11])\r\n  };\r\n  ConfettoUpdate = () => {\r\n    // apply forces to velocity\r\n    this.velocity.x -= this.velocity.x * dragConfetti\r\n    this.velocity.y = Math.min(this.velocity.y + gravityConfetti, terminalVelocity)\r\n    this.velocity.x += Math.random() > 0.5 ? Math.random() : -Math.random()\r\n    \r\n    // set position\r\n    this.position.x += this.velocity.x\r\n    this.position.y += this.velocity.y\r\n  \r\n    // spin confetto by scaling y and set the color, .09 just slows cosine frequency\r\n    this.scale.y = Math.cos((this.position.y + this.randomModifier) * 0.09)   \r\n  };\r\n  // Confetto.prototype.update = function() {\r\n  //   // apply forces to velocity\r\n  //   this.velocity.x -= this.velocity.x * dragConfetti\r\n  //   this.velocity.y = Math.min(this.velocity.y + gravityConfetti, terminalVelocity)\r\n  //   this.velocity.x += Math.random() > 0.5 ? Math.random() : -Math.random()\r\n    \r\n  //   // set position\r\n  //   this.position.x += this.velocity.x\r\n  //   this.position.y += this.velocity.y\r\n  \r\n  //   // spin confetto by scaling y and set the color, .09 just slows cosine frequency\r\n  //   this.scale.y = Math.cos((this.position.y + this.randomModifier) * 0.09)    \r\n  // };\r\n\r\n  Sequin = () => {\r\n    this.color = this.settings.colors[Math.floor(this.variables.randomRange(0, this.settings.colors.length))],\r\n    this.radius = this.variables.randomRange(1, 2),\r\n    this.position = {\r\n      x: this.variables.randomRange(canvas.width/2 - button.offsetWidth/3, canvas.width/2 + button.offsetWidth/3),\r\n      y: this.variables.randomRange(canvas.height/2 + button.offsetHeight/2 + 8, canvas.height/2 + (1.5 * button.offsetHeight) - 8),\r\n    },\r\n    this.velocity = {\r\n      x: this.variables.randomRange(-6, 6),\r\n      y: this.variables.randomRange(-8, -12)\r\n    }\r\n  };\r\n\r\n  SequinUpdate = () => {\r\n    // apply forces to velocity\r\n    this.velocity.x -= this.velocity.x * dragSequins\r\n    this.velocity.y = this.velocity.y + gravitySequins\r\n    \r\n    // set position\r\n    this.position.x += this.velocity.x\r\n    this.position.y += this.velocity.y   \r\n  };\r\n\r\n  initBurst(){\r\n    for (let i = 0; i < this.settings.confettiCount; i++) {\r\n      this.variables.confetti.push(this.Confetto)\r\n    }\r\n    for (let i = 0; i < this.settings.sequinCount; i++) {\r\n      this.variables.sequins.push(this.Sequin)\r\n    }\r\n    console.log(this.variables.confetti)\r\n  };\r\n\r\n  render = () => {\r\n    this.elements.ctx.clearRect(0, 0, canvas.width, canvas.height)\r\n\r\n    this.variables.confetti.forEach((confetto, index) => {\r\n      console.log(\"dimensions: \", confetto);\r\n      let width = (confetto.dimensions.x * confetto.scale.x)\r\n      let height = (confetto.dimensions.y * confetto.scale.y)\r\n      \r\n      // move canvas to position and rotate\r\n      this.elements.ctx.translate(confetto.position.x, confetto.position.y)\r\n      this.elements.ctx.rotate(confetto.rotation)\r\n  \r\n      // update confetto \"physics\" values\r\n      this.ConfettoUpdate\r\n      \r\n      // get front or back fill color\r\n      this.elements.ctx.fillStyle = confetto.scale.y > 0 ? confetto.color.front : confetto.color.back\r\n      \r\n      // draw confetto\r\n      this.elements.ctx.fillRect(-width / 2, -height / 2, width, height)\r\n      \r\n      // reset transform matrix\r\n      this.elements.ctx.setTransform(1, 0, 0, 1, 0, 0)\r\n  \r\n      // clear rectangle where button cuts off\r\n      if (confetto.velocity.y < 0) {\r\n        this.elements.ctx.clearRect(canvas.width/2 - button.offsetWidth/2, canvas.height/2 + button.offsetHeight/2, button.offsetWidth, button.offsetHeight)\r\n      }\r\n    })\r\n  \r\n    this.variables.sequins.forEach((sequin, index) => {  \r\n      // move canvas to position\r\n      this.elements.ctx.translate(sequin.position.x, sequin.position.y)\r\n      \r\n      // update sequin \"physics\" values\r\n      this.SequinUpdate;\r\n      \r\n      // set the color\r\n      this.elements.ctx.fillStyle = sequin.color\r\n      \r\n      // draw sequin\r\n      this.elements.ctx.beginPath()\r\n      this.elements.ctx.arc(0, 0, sequin.radius, 0, 2 * Math.PI)\r\n      this.elements.ctx.fill()\r\n  \r\n      // reset transform matrix\r\n      this.elements.ctx.setTransform(1, 0, 0, 1, 0, 0)\r\n  \r\n      // clear rectangle where button cuts off\r\n      if (sequin.velocity.y < 0) {\r\n        this.elements.ctx.clearRect(canvas.width/2 - button.offsetWidth/2, canvas.height/2 + button.offsetHeight/2, button.offsetWidth, button.offsetHeight)\r\n      }\r\n    })\r\n  \r\n    // remove confetti and sequins that fall off the screen\r\n    // must be done in seperate loops to avoid noticeable flickering\r\n    this.variables.confetti.forEach((confetto, index) => {\r\n      if (confetto.position.y >= canvas.height) this.variables.confetti.splice(index, 1)\r\n    })\r\n    this.variables.sequins.forEach((sequin, index) => {\r\n      if (sequin.position.y >= canvas.height) this.variables.sequins.splice(index, 1)\r\n    })\r\n  \r\n    window.requestAnimationFrame(render)\r\n  };\r\n\r\n  clickButton = () => {\r\n    if (!disabled) {\r\n      disabled = true\r\n      // Loading stage\r\n      button.classList.add('loading')\r\n      button.classList.remove('ready')\r\n      setTimeout(() => {\r\n        // Completed stage\r\n        button.classList.add('complete')\r\n        button.classList.remove('loading')\r\n        setTimeout(() => {\r\n          this.initBurst;\r\n          setTimeout(() => {\r\n            // Reset button so user can select it again\r\n            disabled = false\r\n            button.classList.add('ready')\r\n            button.classList.remove('complete')\r\n          }, 4000)\r\n        }, 320)\r\n      }, 1800)\r\n    }\r\n  };\r\n\r\n  resizeCanvas = () => {\r\n    canvas.width = window.innerWidth\r\n    canvas.height = window.innerHeight\r\n    cx = this.elements.ctx.canvas.width / 2\r\n    cy = this.elements.ctx.canvas.height / 2\r\n  };\r\n\r\n  resize = () => {\r\n    window.addEventListener('resize', () => {\r\n      this.resizeCanvas\r\n    });\r\n  };\r\n};\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n    // ammount to add on each button press\r\n    const confettiCount = 20\r\n    const sequinCount = 10\r\n    \r\n    // \"physics\" variables\r\n    const gravityConfetti = 0.3\r\n    const gravitySequins = 0.55\r\n    const dragConfetti = 0.075\r\n    const dragSequins = 0.02\r\n    const terminalVelocity = 3\r\n    \r\n    // init other global elements\r\n    const button = document.getElementById('button')\r\n    var disabled = false\r\n    const canvas = document.getElementById('canvas')\r\n    const ctx = canvas.getContext('2d')\r\n    canvas.width = window.innerWidth\r\n    canvas.height = window.innerHeight\r\n    let cx = ctx.canvas.width / 2\r\n    let cy = ctx.canvas.height / 2\r\n    \r\n    // add Confetto/Sequin objects to arrays to draw them\r\n    let confetti = []\r\n    let sequins = []\r\n    \r\n    // colors, back side is darker for confetti flipping\r\n    const colors = [\r\n      { front : '#7b5cff', back: '#6245e0' }, // Purple\r\n      { front : '#b3c7ff', back: '#8fa5e5' }, // Light Blue\r\n      { front : '#5c86ff', back: '#345dd1' }  // Darker Blue\r\n    ]\r\n    \r\n    // helper function to pick a random number within a range\r\n    const randomRange = (min, max) => Math.random() * (max - min) + min\r\n    \r\n    // helper function to get initial velocities for confetti\r\n    // this weighted spread helps the confetti look more realistic\r\n    const initConfettoVelocity = (xRange, yRange) => {\r\n      const x = randomRange(xRange[0], xRange[1])\r\n      const range = yRange[1] - yRange[0] + 1\r\n      let y = yRange[1] - Math.abs(randomRange(0, range) + randomRange(0, range) - range)\r\n      if (y >= yRange[1] - 1) {\r\n        // Occasional confetto goes higher than the max\r\n        y += (Math.random() < .25) ? randomRange(1, 3) : 0\r\n      }\r\n      return {x: x, y: -y}\r\n    }\r\n    \r\n    // Confetto Class\r\n    function Confetto() {\r\n      this.randomModifier = randomRange(0, 99)\r\n      this.color = colors[Math.floor(randomRange(0, colors.length))]\r\n      this.dimensions = {\r\n        x: randomRange(5, 9),\r\n        y: randomRange(8, 15),\r\n      }\r\n      this.position = {\r\n        x: randomRange(canvas.width/2 - button.offsetWidth/4, canvas.width/2 + button.offsetWidth/4),\r\n        y: randomRange(canvas.height/2 + button.offsetHeight/2 + 8, canvas.height/2 + (1.5 * button.offsetHeight) - 8),\r\n      }\r\n      this.rotation = randomRange(0, 2 * Math.PI)\r\n      this.scale = {\r\n        x: 1,\r\n        y: 1,\r\n      }\r\n      this.velocity = initConfettoVelocity([-9, 9], [6, 11])\r\n    }\r\n    Confetto.prototype.update = function() {\r\n      // apply forces to velocity\r\n      this.velocity.x -= this.velocity.x * dragConfetti\r\n      this.velocity.y = Math.min(this.velocity.y + gravityConfetti, terminalVelocity)\r\n      this.velocity.x += Math.random() > 0.5 ? Math.random() : -Math.random()\r\n      \r\n      // set position\r\n      this.position.x += this.velocity.x\r\n      this.position.y += this.velocity.y\r\n    \r\n      // spin confetto by scaling y and set the color, .09 just slows cosine frequency\r\n      this.scale.y = Math.cos((this.position.y + this.randomModifier) * 0.09)    \r\n    }\r\n    \r\n    // Sequin Class\r\n    function Sequin() {\r\n      this.color = colors[Math.floor(randomRange(0, colors.length))].back,\r\n      this.radius = randomRange(1, 2),\r\n      this.position = {\r\n        x: randomRange(canvas.width/2 - button.offsetWidth/3, canvas.width/2 + button.offsetWidth/3),\r\n        y: randomRange(canvas.height/2 + button.offsetHeight/2 + 8, canvas.height/2 + (1.5 * button.offsetHeight) - 8),\r\n      },\r\n      this.velocity = {\r\n        x: randomRange(-6, 6),\r\n        y: randomRange(-8, -12)\r\n      }\r\n    }\r\n    Sequin.prototype.update = function() {\r\n      // apply forces to velocity\r\n      this.velocity.x -= this.velocity.x * dragSequins\r\n      this.velocity.y = this.velocity.y + gravitySequins\r\n      \r\n      // set position\r\n      this.position.x += this.velocity.x\r\n      this.position.y += this.velocity.y   \r\n    }\r\n    \r\n    // add elements to arrays to be drawn\r\n    const initBurst = () => {\r\n      for (let i = 0; i < confettiCount; i++) {\r\n        confetti.push(new Confetto())\r\n      }\r\n      for (let i = 0; i < sequinCount; i++) {\r\n        sequins.push(new Sequin())\r\n      }\r\n    }\r\n    \r\n    // draws the elements on the canvas\r\n    const render = () => {\r\n      ctx.clearRect(0, 0, canvas.width, canvas.height)\r\n      \r\n      confetti.forEach((confetto, index) => {\r\n        let width = (confetto.dimensions.x * confetto.scale.x)\r\n        let height = (confetto.dimensions.y * confetto.scale.y)\r\n        \r\n        // move canvas to position and rotate\r\n        ctx.translate(confetto.position.x, confetto.position.y)\r\n        ctx.rotate(confetto.rotation)\r\n    \r\n        // update confetto \"physics\" values\r\n        confetto.update()\r\n        \r\n        // get front or back fill color\r\n        ctx.fillStyle = confetto.scale.y > 0 ? confetto.color.front : confetto.color.back\r\n        \r\n        // draw confetto\r\n        ctx.fillRect(-width / 2, -height / 2, width, height)\r\n        \r\n        // reset transform matrix\r\n        ctx.setTransform(1, 0, 0, 1, 0, 0)\r\n    \r\n        // clear rectangle where button cuts off\r\n        if (confetto.velocity.y < 0) {\r\n          ctx.clearRect(canvas.width/2 - button.offsetWidth/2, canvas.height/2 + button.offsetHeight/2, button.offsetWidth, button.offsetHeight)\r\n        }\r\n      })\r\n    \r\n      sequins.forEach((sequin, index) => {  \r\n        // move canvas to position\r\n        ctx.translate(sequin.position.x, sequin.position.y)\r\n        \r\n        // update sequin \"physics\" values\r\n        sequin.update()\r\n        \r\n        // set the color\r\n        ctx.fillStyle = sequin.color\r\n        \r\n        // draw sequin\r\n        ctx.beginPath()\r\n        ctx.arc(0, 0, sequin.radius, 0, 2 * Math.PI)\r\n        ctx.fill()\r\n    \r\n        // reset transform matrix\r\n        ctx.setTransform(1, 0, 0, 1, 0, 0)\r\n    \r\n        // clear rectangle where button cuts off\r\n        if (sequin.velocity.y < 0) {\r\n          ctx.clearRect(canvas.width/2 - button.offsetWidth/2, canvas.height/2 + button.offsetHeight/2, button.offsetWidth, button.offsetHeight)\r\n        }\r\n      })\r\n    \r\n      // remove confetti and sequins that fall off the screen\r\n      // must be done in seperate loops to avoid noticeable flickering\r\n      confetti.forEach((confetto, index) => {\r\n        if (confetto.position.y >= canvas.height) confetti.splice(index, 1)\r\n      })\r\n      sequins.forEach((sequin, index) => {\r\n        if (sequin.position.y >= canvas.height) sequins.splice(index, 1)\r\n      })\r\n    \r\n      window.requestAnimationFrame(render)\r\n    }\r\n    \r\n    // cycle through button states when clicked\r\n    const clickButton = () => {\r\n      if (!disabled) {\r\n        disabled = true\r\n        // Loading stage\r\n        button.classList.add('loading')\r\n        button.classList.remove('ready')\r\n        setTimeout(() => {\r\n          // Completed stage\r\n          button.classList.add('complete')\r\n          button.classList.remove('loading')\r\n          setTimeout(() => {\r\n            initBurst()\r\n            setTimeout(() => {\r\n              // Reset button so user can select it again\r\n              disabled = false\r\n              button.classList.add('ready')\r\n              button.classList.remove('complete')\r\n            }, 4000)\r\n          }, 320)\r\n        }, 1800)\r\n      }\r\n    }\r\n    \r\n    // re-init canvas if the window size changes\r\n    const resizeCanvas = () => {\r\n      canvas.width = window.innerWidth\r\n      canvas.height = window.innerHeight\r\n      cx = ctx.canvas.width / 2\r\n      cy = ctx.canvas.height / 2\r\n    }\r\n    \r\n    // resize listenter\r\n    window.addEventListener('resize', () => {\r\n      resizeCanvas()\r\n    })\r\n    \r\n    // click button on spacebar or return keypress\r\n    document.body.onkeyup = (e) => {\r\n      if (e.keyCode == 13 || e.keyCode == 32) {\r\n        clickButton()\r\n      }\r\n    }\r\n    \r\n    // Set up button text transition timings on page load\r\n    const textElements = button.querySelectorAll('.button-text')\r\n    textElements.forEach((element) => {\r\n      const characters = element.innerText.split('')\r\n      let characterHTML = ''\r\n      characters.forEach((letter, index) => {\r\n        characterHTML += `<span class=\"char${index}\" style=\"--d:${index * 30}ms; --dr:${(characters.length - index - 1) * 30}ms;\">${letter}</span>`\r\n      })\r\n      element.innerHTML = characterHTML\r\n    })\r\n    \r\n    // kick off the render loop\r\n    initBurst()\r\n    render()\r\n    clickButton();\r\n\r\ncustomElements.define('confetti-animation', ConfettiAnimation);\n\n//# sourceURL=webpack://shopify-theme-starter/./src/js/modules/test.js?");

/***/ })

}]);