<div class="flex flex-1 min-h-0 flex-col" data-cart-drawer-content data-cart-item-count="{{ cart.item_count }}">
  <div class="flex-shrink-0 px-5 pt-4 pb-2{% if cart.item_count == 0 %} hidden{% endif %}">
    {%- render 'cart-tier-bar' -%}
  </div>

  <div class="flex-1 overflow-y-auto min-h-0" data-cart-items>
    {% if cart.item_count > 0 %}
      <div class="cart_items_container px-4 pb-2">
        {%- render 'cart-drawer-items' -%}
      </div>
      {%- render 'cart-recommendations' -%}
    {% else %}
      {%- render 'plaay-cart-empty-state' -%}
    {% endif %}
  </div>

  <div class="flex-shrink-0 border-t border-dark-blue border-opacity-10 px-5 pt-4 pb-5">
    {% if cart.item_count > 0 %}
      <div data-cart-drawer-footer>
        {%- render 'cart-drawer-footer' -%}
      </div>

      <a
        href="/checkout"
        class="plaay-cart-checkout-btn font-walsheim-bold uppercase">
        {{ 'cart-drawer.checkout' | t }}
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>

      <button
        type="button"
        class="plaay-cart-continue-btn font-walsheim-regular"
        data-close>
        {{ 'cart-drawer.continue_shopping' | t }}
      </button>
    {% else %}
      <button
        type="button"
        class="plaay-cart-checkout-btn font-walsheim-bold uppercase"
        data-close>
        Keep Browsing
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </button>
    {% endif %}
  </div>
</div>
